import 'package:flutter_test/flutter_test.dart';

void main() {
  test('localization package is reserved for future l10n exports', () {
    // See docs/LOCALIZATION.md — ARB generation not wired in app yet.
    expect(true, isTrue);
  });
}
