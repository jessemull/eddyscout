include: ../../../../../../tooling/analysis_options.feature.yaml

# Presentation may use relative imports to sibling data/ within the feature.
linter:
  rules:
    always_use_package_imports: false
    public_member_api_docs: false
