name: eddyscout_persistence
description: Local storage abstractions for key-value and structured data.
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  eddyscout_core:
    path: ../core
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^7.0.0
