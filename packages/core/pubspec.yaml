name: eddyscout_core
description: Core utilities, types, and abstractions for the EddyScout workspace.
publish_to: 'none'
version: 0.1.0

resolution: workspace

environment:
  sdk: ^3.11.4

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  meta: ^1.17.0

dev_dependencies:
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0
