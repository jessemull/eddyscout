include: analysis_options.package.yaml

# Style-only rules deferred (low value vs churn); all other M6 relaxations removed.
analyzer:
  errors:
    prefer_constructors_over_static_methods: ignore
    prefer_expression_function_bodies: ignore
