rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Writes only from Admin SDK (Cloud Functions). Clients use Callables.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
